import {
    SidePanel as SidePanelDumb,
    SidePanelSmart,
    sidePanelLifecycle,
    BG,
} from '../SidePanel.jsx'

describe('SidePanelDumb.render', () => {
    test('render with correct props', () => {
        expect(1).toEqual(1)
    })
})
