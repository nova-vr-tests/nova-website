declare var document: { // eslint-disable-line no-unused-vars
    documentElement: {
        clientHeight: number,
        clientWidth: number,
    },

    // fbjs uses these
    body: {
        clientHeight: number,
        clientWidth: number,
    }
}
